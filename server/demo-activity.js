import {mutate,id} from './store.js';
import {book} from './domain.js';
export async function seedActivity(){await mutate(db=>{if(db.procurements.some(p=>p.demoSeed))return;const owner=db.users.find(u=>u.email==='farmer@demo.ekharid.in');if(!owner)return;for(let i=0;i<18;i++){const name=['Suresh Patel','Kamala Devi','Ramesh Yadav','Sunita Verma','Harish Singh','Geeta Sharma'][i%6];const centre=db.procurementCentres[i%4];let f=i%3===0?owner:db.users.find(u=>u.email===`farmer${i}@example.test`);if(!f){f={_id:id(),name,email:`farmer${i}@example.test`,mobile:`980001${String(i).padStart(4,'0')}`,passwordHash:owner.passwordHash,role:'farmer',state:centre.state,district:centre.district,status:'ACTIVE',createdAt:new Date().toISOString()};db.users.push(f);db.farmers.push({_id:id(),userId:f._id,farmerId:`EK-UP-${10020+i}`,village:'Malihabad',verificationStatus:i%4===0?'PENDING':'VERIFIED',state:f.state,district:f.district});}const day=new Date(Date.now()-(i%7)*86400000).toISOString();const crop=db.crops[i%3],quantity=8+i*1.5;const slot={_id:id(),farmerId:f._id,farmerName:f.name,centreId:centre._id,centreName:centre.name,state:centre.state,district:centre.district,crop:crop.name,quantity,date:day.slice(0,10),timeSlot:'09:00',status:'COMPLETED',tokenNumber:`H${201+i}`,createdAt:day,demoSeed:true};db.slots.push(slot);db.queueTokens.push({...slot});const p={...slot,_id:id(),slotId:slot._id,quality:'A',price:crop.price,totalAmount:Math.round(quantity*crop.price*100)/100,verifiedBy:db.users.find(u=>u.role==='district')._id};db.procurements.push(p);db.payments.push({_id:id(),farmerId:f._id,farmerName:f.name,procurementId:p._id,state:p.state,district:p.district,amount:p.totalAmount,status:i%4===0?'PROCESSING':'COMPLETED',transactionId:i%4===0?null:`DEMO-UTR-${100020+i}`,paymentDate:i%4===0?null:day,createdAt:day,demoSeed:true});}if(!db.slots.some(s=>s.farmerId===owner._id&&!['COMPLETED','CANCELLED'].includes(s.status)))book(db,owner,{centreId:'centre-0',crop:'Wheat',quantity:24,date:new Date(Date.now()+86400000).toISOString().slice(0,10),timeSlot:'09:00'});});}
