import {useEffect,useState} from 'react';
import {Routes,Route,Navigate} from 'react-router-dom';
import Landing from './pages/Landing';
import {Login,Register,Forgot} from './pages/Auth';
import Dashboard from './pages/Dashboard';
import {api} from './services/api';
import {Loading} from './components/UI';
export default function App(){const [user,setUser]=useState(null),[ready,setReady]=useState(false),[config,setConfig]=useState(null);useEffect(()=>{api.get('/config').then(r=>setConfig(r.data)).catch(()=>{});api.get('/auth/me').then(r=>setUser(r.data)).catch(async()=>{try{await api.post('/auth/refresh');setUser((await api.get('/auth/me')).data);}catch{}}).finally(()=>setReady(true));const expire=()=>setUser(null);window.addEventListener('session-expired',expire);return()=>window.removeEventListener('session-expired',expire);},[]);if(!ready)return <Loading/>;return <Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={user?<Navigate to="/app"/>:<Login onLogin={setUser} config={config}/>}/><Route path="/register" element={user?<Navigate to="/app"/>:<Register onLogin={setUser}/>}/><Route path="/forgot-password" element={<Forgot/>}/><Route path="/app/*" element={user?<Dashboard user={user} onLogout={()=>setUser(null)} config={config}/>:<Navigate to="/login" replace/>}/><Route path="/privacy" element={<div className="privacy-page"><h1>Privacy & terms</h1><p>e-Kharid is a Smart India Hackathon prototype, not an official government portal. Use synthetic information in demo mode.</p><h2>Information and purpose</h2><p>Registration details, encrypted identity information, documents and procurement records support account verification, booking and procurement tracking. Officials may access records within their assigned jurisdiction. Identity values are masked in profile responses.</p><h2>Storage and access</h2><p>Documents and identity values are encrypted at rest. Authentication uses HTTP-only session cookies. Deployment operators must configure secure hosting, access controls, retention periods and service providers before processing real farmer data.</p><h2>Your choices</h2><p>Contact your district procurement office or the deployment operator to request corrections, access or deletion. Operational and audit records may need to be retained according to the deployment's applicable policy.</p><h2>Service terms</h2><p>Slot availability depends on centre capacity. Payment records reflect updates by authorized officials; this application does not transfer money. No payment date is guaranteed.</p><a href="/">Back to e-Kharid</a></div>}/><Route path="*" element={<div className="empty"><h1>Page not found</h1><a href="/">Return home</a></div>}/></Routes>;}
